# CORRECT CHLOROPHYLL CONFIGURATION FOR COPERNICUS
# The variable name at the end MUST be lowercase "chl" not "CHL"

CMEMS_CHL_WMTS_TEMPLATE=https://wmts.marine.copernicus.eu/teroWmts?SERVICE=WMTS&REQUEST=GetTile&VERSION=1.0.0&LAYER=OCEANCOLOUR_GLO_BGC_L4_NRT_009_102/cmems_obs-oc_glo_bgc-plankton_nrt_l4-gapfree-multi-4km_P1D/chl&STYLE=default&FORMAT=image/png&TILEMATRIXSET=EPSG:3857&TILEMATRIX={z}&TILEROW={y}&TILECOL={x}&TIME={TIME}

# Key differences from the broken version:
# 1. "/chl" at the end (lowercase, not "/CHL")
# 2. STYLE=default (not cmap:jet which may not exist for this layer)

# Your existing auth credentials (these are correct):
COPERNICUS_USER=jrosenkilde
COPERNICUS_PASS=Alwaysbent82886!

# TO APPLY THIS FIX:
# 1. Update your .env.local with the corrected CMEMS_CHL_WMTS_TEMPLATE above
# 2. Update Vercel environment variables with the same
# 3. Restart your dev server or redeploy

# TEST THE FIX:
# Visit your analysis page and toggle the CHL layer
# It should now display chlorophyll data using Copernicus, exactly like SST works
