// NOTE: App chrome is now provided by src/app/legendary/layout.tsx.
// This stub remains only for any legacy imports to avoid breakage.
export default function LegendaryShell({ children }: { children?: React.ReactNode }) {
  return <>{children}</>;
}
