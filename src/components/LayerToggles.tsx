'use client';

import { useEffect } from "react";
import { useMapbox } from "@/lib/MapCtx";
import { useAppState } from "@/store/appState";

// Duplicate toggle UI removed; HeaderBar is the single source of truth.
export default function LayerToggles() { return null; }




